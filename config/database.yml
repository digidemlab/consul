default: &default
  adapter: postgresql
  encoding: unicode
  host: localhost
  pool: 5
  username:
  password:

development: &development
  <<: *default
  database: consul_development_gbg_ml

# The staging, preproduction and production dbs are only needed
# for running rake assets:precompile locally before deploying
staging:
  <<: *default
  database: consul_staging

preproduction:
  <<: *development

production:
  <<: *default
  database: consul_production

test:
  <<: *default
  database: consul_test
